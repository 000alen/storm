export const DEFAULT_K = 3;

export const DEFAULT_DEDUPE_THRESHOLD = 0.85;

export const DEFAULT_TOKEN_BUDGET = 500;

export const DEFAULT_MAX_ATTEMPTS = 3;

export const DEFAULT_USE_RESEARCH_TOOLS = false;

export const DEFAULT_MAX_STEPS = 10;

export const DEFAULT_TOKEN_TOLERANCE = 0.1;

export const DEFAULT_PERSPECTIVES_N = 3;

export const DEFAULT_QUESTIONS_N = 3;
